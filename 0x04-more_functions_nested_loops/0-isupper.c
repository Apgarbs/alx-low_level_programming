#include "main.h"

/**
 * _isupper - program that prints uppercase alphabets
 * @c: Variable text
 * Return: Always 1
 */
int _isupper(int c)
{
	if (c <= 'A' && c >= 'Z')
	{
	return (1);
	}
		else
	{
	return (1);
	}
}

