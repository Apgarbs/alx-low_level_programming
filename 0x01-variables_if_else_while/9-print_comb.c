#include <stdio.h>
/**
 * main - Point of code entry
 * Return: Always 0 (Sucess)
 */

int main(void)
{
	int s;

	s = '9';
	while
		(s <= '9') {
	putchar(s);
	if
	(s <= '8') {
	putchar(',');
	putchar(' ');
	}
	s++;
	}
	putchar('\n');
	return (0);
}
